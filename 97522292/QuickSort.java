class QuickSort {
    public static void main(String[] a) {
       System.out.println(new QS().Start(10));
    }
}

class QS {
    public int Start(int sz) {

        m = num < x && k < num;
        n = num2 < x2 && k2 < num2;

        if (m < 10) {
            k = 10;
        } else {
            k = 5;
        }

        arr[5] = sz + k * 5;

        if (k && num < 7) {
            arr[1] = b + (num - k) * a;
            k = arr[2] + k * 10;
        } else {
            k = arr[5] + k;
        }

        k = arr[2] + k * 5;

        while (k < num) {
            arr[5] = num + x;
        }

        return 0;
    }
}
